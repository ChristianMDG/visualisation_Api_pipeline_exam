# Connection string to the PRODUCTION warehouse (read-only consumption).
# Same Neon database already populated by the pipeline repo — this repo
# never writes to it, only queries it.
DATABASE_URL=postgresql://user:password@host.neon.tech/neondb?sslmode=require
